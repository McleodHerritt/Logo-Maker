// Importing the CLI class from the cli.js file located in the lib directory
const CLI = require("./lib/cli.js");

// Creating a new instance of the CLI class
const cli = new CLI();

// Running the CLI instance to start the user interaction
cli.run();
